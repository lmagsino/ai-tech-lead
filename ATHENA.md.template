# ATHENA — Project Constitution

> Copy this file to your project root as `ATHENA.md` and fill it in.
> Keep it honest and brief. ATHENA reads this every session.

---

## Project

**Name:** [Your project name]
**Description:** [One sentence: what it does and who it's for]
**Stage:** [e.g. MVP / Beta / Growth]

---

## Stack

- **Language(s):** [e.g. TypeScript]
- **Frontend:** [e.g. Next.js 14, Tailwind CSS]
- **Backend:** [e.g. Next.js API routes, tRPC]
- **Database:** [e.g. PostgreSQL via Supabase]
- **Cache / Queue:** [e.g. Redis, BullMQ]
- **Infrastructure:** [e.g. Vercel, Fly.io, AWS]
- **Auth:** [e.g. Supabase Auth, Clerk, NextAuth]

---

## AI Components

> Document every AI component in the product. ATHENA uses this to make AI-first suggestions.

- **LLM:** [e.g. Claude via Anthropic SDK — used for X, Y, Z]
- **Embeddings:** [e.g. OpenAI text-embedding-3-small via pgvector]
- **Vector store:** [e.g. pgvector on Supabase]
- **Agents:** [e.g. none / Claude tool use for Y]
- **Evals:** [e.g. Vitest-based eval suite in src/ai/evals/]

---

## Architecture

[Describe the high-level structure in 3-5 lines. Example:]

- Next.js monorepo — frontend and API routes in one repo
- AI services isolated in src/ai/ — client, prompts, schemas, evals
- PostgreSQL as primary store with pgvector for semantic search
- All AI calls go through a single client with timeout and retry handling

---

## Rules

> Only list things ATHENA gets wrong without being told. Max 10.

1. [e.g. All AI prompts live in src/ai/prompts/ — never inline in business logic]
2. [e.g. All AI outputs validated against Zod schemas before use]
3. [e.g. Every AI endpoint has a rate limit and a fallback]
4. [e.g. PII is stripped before sending to any external AI API]
5. [e.g. TypeScript strict mode — no any]
6.
7.
8.
9.
10.

---

## How to verify

```bash
# Run tests
[e.g. pnpm test]

# Run linter
[e.g. pnpm lint]

# Type check
[e.g. pnpm typecheck]

# Start dev server
[e.g. pnpm dev]
```

---

## Conventions

- **Branch naming:** [e.g. `feature/`, `fix/`, `chore/`]
- **Commit format:** [e.g. Conventional Commits — `feat:`, `fix:`, `chore:`]
- **Spec location:** `specs/` — one file per feature, approved before implementation
